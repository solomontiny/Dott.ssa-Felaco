fastapi==0.110.1
uvicorn==0.25.0
motor==3.3.1
pymongo==4.5.0
python-dotenv==1.2.1
bcrypt==4.1.3
PyJWT==2.11.0
python-multipart==0.0.22
pydantic==2.12.5
python-dateutil==2.9.0
requests==2.32.5
pytest==9.0.2

# Minimal, validated requirements for the backend. Do NOT add private or Emergent-only packages.
# If additional packages are required, add them intentionally and verify they exist on PyPI.
