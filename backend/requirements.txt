# Python backend removed — Supabase will be used instead.
# requirements.txt left intentionally minimal to avoid accidental pip installs.
# If you still need the previous Python backend, restore the file from git history.
